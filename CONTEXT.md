# Repo CONTEXT.md (shim)

The pipeline’s routing/orientation context lives at `icm/CONTEXT.md`.

- Read `icm/CLAUDE.md` for global rules.
- Read `icm/CONTEXT.md` to determine the current stage and required inputs/outputs.

